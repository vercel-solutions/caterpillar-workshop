// TODO: Build the task detail page
// This page should show full details for a single task
//
// Use your AI tool to:
// 1. Get the task ID from the route params
// 2. Fetch the task data from the API (see src/api.ts)
// 3. Fetch the assignee info using getUser()
// 4. Display all task fields with proper formatting (title, description, status, priority, tags, assignee, date)
// 5. Add a back link to the board
// 6. Add proper metadata using generateMetadata
// 7. Handle the case where the task ID doesn't exist (return notFound())

export default function TaskDetailPage() {
  return (
    <div>
      <p className="text-muted-foreground">
        Task detail — implement with AI assistance
      </p>
    </div>
  )
}
